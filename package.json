{
  "name": "wellness-shop",
  "version": "1.0.0",
  "description": "Simple wellness shop for tracking fitness sessions and redeeming rewards",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "nuxt generate && gh-pages -d docs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxtjs/supabase": "^1.2.2",
    "@pinia/nuxt": "^0.5.1",
    "nuxt": "^3.11.2",
    "nuxt-icon": "^0.6.10",
    "pinia": "^2.1.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "gh-pages": "^6.0.0",
    "typescript": "^5.4.5"
  }
}
